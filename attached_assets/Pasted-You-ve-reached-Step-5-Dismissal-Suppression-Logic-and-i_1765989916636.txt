You’ve reached Step 5 — Dismissal & Suppression Logic, and it’s one of the most critical steps because:

👉 You’re now teaching the system how to stay quiet after being heard.

Let’s build this exactly as described — light, respectful, non-ceremonial.

🎯 What We’re Adding

We'll implement:

✅ A lightweight dismissal record store

✅ Suppression check: “has this insight already been acknowledged?”

✅ Logic to not log duplicate drift for the same step & pattern

✅ Simple 7-day suppression window

❌ No UI yet

❌ No user-based state

❌ No dashboard, settings, or snooze buttons

🧠 Memory: What Should Be Remembered?

Each suppressed insight needs this:

{
  patternType: "cycle-time-drift",
  entityId: "routing-step-123",
  dismissedAt: "2025-12-17T12:00:00Z",
  expiresAt: "2025-12-24T12:00:00Z"
}

📁 New File: suppressionStore.js

A simple in-memory store (or mock local file DB if you want persistence later):

const suppressionMemory = new Map(); // key: stepId|patternType

const SUPPRESSION_DAYS = 7;
const MS_IN_DAY = 24 * 60 * 60 * 1000;

export function isSuppressed(stepId, patternType = 'cycle-time-drift') {
  const key = `${stepId}|${patternType}`;
  const record = suppressionMemory.get(key);

  if (!record) return false;

  const now = Date.now();
  return now < new Date(record.expiresAt).getTime();
}

export function suppress(stepId, patternType = 'cycle-time-drift') {
  const key = `${stepId}|${patternType}`;
  const now = new Date();
  const expires = new Date(now.getTime() + SUPPRESSION_DAYS * MS_IN_DAY);

  suppressionMemory.set(key, {
    patternType,
    entityId: stepId,
    dismissedAt: now.toISOString(),
    expiresAt: expires.toISOString(),
  });
}

🔁 Update driftLogic.js

Update to check suppression before logging, and suppress after logging:

import { isSuppressed, suppress } from './suppressionStore.js';

export function detectDrift(stepId, instances) {
  if (instances.length < 15) return null;

  const durations = instances
    .map(d => new Date(d.completedAt) - new Date(d.startedAt))
    .filter(d => d > 0);

  if (durations.length < 15) return null;

  const recent = durations.slice(-5);
  const baselineSet = durations.slice(-15, -5);
  const baselineAvg = baselineSet.reduce((sum, val) => sum + val, 0) / baselineSet.length;
  const threshold = baselineAvg * 1.25;

  const driftCount = recent.filter(d => d > threshold).length;

  if (driftCount >= 3) {
    if (isSuppressed(stepId)) {
      // Already seen and acknowledged → stay silent
      return null;
    }

    // Confidence level
    let confidence = 'LOW';
    if (driftCount === 4) confidence = 'MEDIUM';
    if (driftCount === 5) confidence = 'HIGH';

    // Log with metadata
    console.log(`\n[Drift Detected] Step: ${stepId}`);
    console.log(`  🔍 Baseline Avg: ${(baselineAvg / 1000).toFixed(2)} sec (window: 10)`);
    console.log(`  🔍 Threshold (+25%): ${(threshold / 1000).toFixed(2)} sec`);
    console.log(`  🕒 Recent Durations (sec): [${recent.map(d => (d / 1000).toFixed(2)).join(', ')}]`);
    console.log(`  📊 Steps above threshold: ${driftCount} of 5`);
    console.log(`  🎯 Confidence: ${confidence}`);
    console.log(`  📎 Insight suppressed for next 7 days unless resolved.\n`);

    suppress(stepId);

    return {
      stepId,
      driftCount,
      recentDurations: recent,
      baselineAvg,
      confidence,
      message: 'This step has been taking longer recently.'
    };
  }

  return null;
}