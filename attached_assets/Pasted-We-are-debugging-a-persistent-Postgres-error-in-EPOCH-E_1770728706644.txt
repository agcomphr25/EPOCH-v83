We are debugging a persistent Postgres error in EPOCH:

ERROR:
invalid input syntax for type real
parameter $21 = ''
column 21 = custom_discount_value (real)

I need a FULL STRUCTURAL AUDIT of the order creation flow.

DO NOT modify anything yet.
DO NOT apply quick fixes.
DO NOT change drivers.

Your job is to map the entire data flow from frontend to database.

=========================
PHASE 1 — DATABASE LAYER
=========================

1. Print the complete Drizzle schema for:
   - allOrders
   - orderDrafts (if exists)
   - any related insert targets

2. For allOrders:
   - List column order EXACTLY as Drizzle defines it.
   - Identify column position number for each field.
   - Confirm column 21 and its type.

3. Show db.ts:
   - How is drizzle initialized?
   - Are we using Neon HTTP driver?
   - Are we using node-postgres anywhere?
   - Are there multiple DB instances?

4. Confirm if any write paths use different DB instances.

=========================
PHASE 2 — ROUTE HANDLER
=========================

1. Locate createFinalizedOrder (or equivalent).
2. Print entire function.
3. Show:
   - The request body type
   - Any preprocessing
   - Any numeric sanitation
   - The exact insert() call
   - The object passed to insert()

4. Add temporary logging BEFORE the insert:
   Log:
   - typeof customDiscountValue
   - value
   - entire insert object
   - Object.entries(insertObject)

DO NOT modify behavior — logging only.

=========================
PHASE 3 — VALIDATION LAYER
=========================

1. Locate any Zod schemas used for order creation.
2. Print full schema.
3. Identify how customDiscountValue is typed.
4. Identify if numeric coercion is happening.
5. Confirm if empty string is allowed.

=========================
PHASE 4 — FRONTEND FLOW
=========================

1. Locate Order Entry form.
2. Show:
   - State initialization for customDiscountValue
   - Input component
   - onChange handler
   - Submit handler
   - API call payload shape

3. Confirm what is sent when:
   - User clears the discount field
   - User leaves it blank
   - User types 0

=========================
PHASE 5 — PARAMETER MAPPING
=========================

1. Show the compiled SQL generated by Drizzle for the insert.
2. Show parameter array in order.
3. Identify which parameter index corresponds to custom_discount_value.
4. Confirm if $21 aligns with schema order.

=========================
PHASE 6 — MULTIPLE INSERT PATHS
=========================

1. Search entire repo for:
   - insert(allOrders)
   - createFinalizedOrder
   - order creation
2. Confirm there is only ONE insert path.
3. Confirm drafts are not reusing a different path.

=========================
OUTPUT FORMAT REQUIRED
=========================

Respond with:

1. DATABASE FLOW MAP (schema → driver → insert)
2. REQUEST FLOW MAP (frontend → API → validation → insert)
3. EXACT CAUSE ANALYSIS
4. WHETHER THIS IS:
   - Frontend issue
   - Validation issue
   - Insert object issue
   - Driver issue
   - Multiple path issue
5. Recommended structural fix (NOT a patch)

DO NOT implement changes.
Only audit and explain.