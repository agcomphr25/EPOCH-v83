We must isolate the P1 OEM invoice_number migration from unrelated destructive schema changes.

Current problem:
Republish is attempting to apply dangerous unrelated schema changes including:

- deleting barcode_printed from p2_serialized_items with live data
- dropping unrelated tables
- dropping indexes
- altering unrelated IDs
- disabling RLS

These are NOT part of the invoice_number task and must NOT go to production.

Goal:
Create a clean migration containing ONLY the safe additive schema required for:

shipment_records.invoice_number

and preserve the P1 OEM Invoice # persistence implementation.

==================================================
TASK
==================================================

1. Audit the generated migration files for this branch

Identify:
- which migration files include unrelated destructive changes
- which files were generated by schema drift rather than intentional work

2. Remove / isolate all unrelated destructive migrations

Specifically:
DO NOT publish:
- p2_serialized_items barcode_printed deletion
- kiosk_pin_rate_limits changes
- unrelated ID type conversions
- unrelated dropped indexes
- unrelated dropped tables
- unrelated RLS changes

unless they are explicitly required for the P1 invoice task (they should not be)

3. Create a clean migration containing ONLY:

ALTER TABLE shipment_records
ADD COLUMN invoice_number text;

(nullable, safe additive)

and any absolutely required companion indexes if justified

4. Ensure the code changes for:
- invoice persistence
- invoice reuse on reprint
- OEM shipment search by invoice #
- UI invoice display

remain intact

without forcing unrelated schema publication

5. Validate:
Production publish preview should show only the intended safe invoice_number migration

==================================================
IMPORTANT
==================================================

Do NOT allow unrelated destructive schema changes to publish.

This is migration isolation only.

Protect production first.

Return:
- which migration files were removed/isolated
- final clean migration file
- confirmation that republish is now safe