You are implementing a production-safe fix for a Parts Request submission failure.

The error occurs when inserting into the `parts_requests` table:

insert or update on table "parts_requests" violates foreign key constraint "parts_requests_ag_part_number_fkey"

The column `parts_requests.ag_part_number` references:

inventory_items.ag_part_number

The frontend sometimes sends values like "GJ101" that do not exist in the `inventory_items.ag_part_number` column. When this happens the database rejects the insert.

We must implement a **server-side guard** so invalid catalog numbers do not cause a crash.

IMPORTANT RULES
• Do NOT change the database schema
• Do NOT change the frontend
• Do NOT remove the FK constraint
• Only modify the server route logic
• Behavior must remain backward compatible
• If an invalid agPartNumber is sent, the request should still succeed with agPartNumber = null

--------------------------------------------------
PHASE 1 — Locate the route
--------------------------------------------------

Find the API endpoint:

POST /api/inventory/parts-requests

Expected file:

server/src/routes/inventory.ts

Locate the handler that looks like:

router.post('/parts-requests', async (req, res) => {

Inside this handler there should be:

const validated = insertPartsRequestSchema.parse(req.body)

--------------------------------------------------
PHASE 2 — Add FK validation guard
--------------------------------------------------

Immediately after validation and before inserting into the database, add a lookup to verify that the provided agPartNumber exists.

Implement logic exactly like this:

1) Extract the value

let agPartNumber = validated.agPartNumber

2) If it exists, check the inventory table

Query:

SELECT ag_part_number
FROM inventory_items
WHERE ag_part_number = $1
LIMIT 1

3) If the query returns no rows, set the value to null.

4) Update the validated payload before insertion.

--------------------------------------------------
PHASE 3 — Implementation pattern
--------------------------------------------------

Insert the following logic before the database insert call.

Use the Drizzle ORM query style used elsewhere in the file.

Example implementation pattern:

let agPartNumber = validated.agPartNumber

if (agPartNumber) {
  const exists = await db
    .select({ agPartNumber: inventoryItems.agPartNumber })
    .from(inventoryItems)
    .where(eq(inventoryItems.agPartNumber, agPartNumber))
    .limit(1)

  if (!exists.length) {
    agPartNumber = null
  }
}

const insertData = {
  ...validated,
  agPartNumber
}

Then use `insertData` in the db.insert() call instead of `validated`.

--------------------------------------------------
PHASE 4 — Confirm correct insert
--------------------------------------------------

The final insert must resemble:

await db.insert(partsRequests).values(insertData)

--------------------------------------------------
PHASE 5 — Behavior after fix
--------------------------------------------------

Requests will behave as follows:

Case 1 — Valid catalog item
agPartNumber exists in inventory_items  
→ saved normally

Case 2 — Invalid catalog item
agPartNumber does not exist  
→ automatically converted to NULL  
→ insert succeeds

Case 3 — agPartNumber omitted
→ NULL stored

--------------------------------------------------
PHASE 6 — Verification
--------------------------------------------------

After implementing:

1) Restart the server
2) Submit a parts request from the UI
3) Confirm HTTP 201 response
4) Confirm record appears in `parts_requests`
5) Confirm ag_part_number is NULL when invalid

--------------------------------------------------
OUTPUT
--------------------------------------------------

Provide:

1) The exact file modified
2) The final updated route handler code
3) The exact lines added
4) Confirmation that the server compiles successfully