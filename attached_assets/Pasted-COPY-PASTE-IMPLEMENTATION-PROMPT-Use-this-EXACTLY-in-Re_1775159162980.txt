COPY-PASTE IMPLEMENTATION PROMPT

Use this EXACTLY in Replit:

IMPLEMENTATION MODE — CONTROLLED REFACTOR
DO NOT BREAK EXISTING FUNCTIONALITY
DO NOT REMOVE DATA FETCHING LOGIC
DO NOT CHANGE BUSINESS RULES

GOAL:
Unify P1 and P2 packing slip PDF generation into a single shared renderer,
while preserving all existing data pipelines.

This is a rendering-layer refactor only.

--------------------------------------------------
STEP 1 — CREATE SHARED PACKING SLIP DATA CONTRACT
--------------------------------------------------

Create a new file:

server/utils/pdf/types.ts

Define:

export interface PackingSlipItem {
  partNumber?: string;
  description: string;
  quantity: number;
  unitNumber?: string;
  serialNumbers?: string[];
  specifications?: string;
}

export interface PackingSlipData {
  packingSlipNumber: string;
  date: string;

  customerName: string;
  customerAddress?: {
    street?: string;
    street2?: string;
    city?: string;
    state?: string;
    zip?: string;
  };

  poNumber?: string;
  lotNumber?: string;

  items: PackingSlipItem[];

  totalQuantity: number;

  trackingNumber?: string;

  packedBy?: string;
  verifiedBy?: string;
}

--------------------------------------------------
STEP 2 — CREATE SHARED RENDERER
--------------------------------------------------

Create new file:

server/utils/pdf/packingSlipPdf.ts

Create function:

export async function generatePackingSlipPdf(
  data: PackingSlipData
): Promise<Buffer>

REQUIREMENTS:

1. Use pdf-lib
2. IMPORT from pdfConfig.ts:
   - COMPANY_INFO
   - drawStandardHeader
   - drawTableHeader
   - wrapText
   - getMargins (async version, NOT static constants)

3. MUST USE async config getters:
   const margins = await getMargins()

4. Layout:

HEADER:
- Use drawStandardHeader()

SHIP TO BLOCK:
- customerName
- address lines

INFO ROW:
- Packing Slip #
- Date
- PO #
- Lot #

TABLE:
Columns:
- Part #
- Description
- Qty
- Serial / Unit

LOOP items:
- Render description + specs if present
- Wrap text using wrapText()

FOOTER:
- Total Quantity
- Packed By
- Verified By
- Tracking #

5. Return PDF as Buffer

--------------------------------------------------
STEP 3 — MAP P1 DATA → SHARED CONTRACT
--------------------------------------------------

Modify:

server/src/routes/poShippingQC.ts

FIND the existing packing slip generation block.

DO NOT DELETE IT YET.

Inside the loop where PDFs are created:

REPLACE ONLY THE PDF CREATION LOGIC WITH:

1. Build PackingSlipData:

const packingData: PackingSlipData = {
  packingSlipNumber: invoiceNumber,
  date: new Date().toLocaleDateString(),

  customerName,
  customerAddress,

  poNumber,

  items: orders.map(order => ({
    description: order.itemName,
    quantity: 1,
    unitNumber: order.unitNumber,
    specifications: order.specifications
  })),

  totalQuantity: orders.length
}

2. CALL:

const pdfBuffer = await generatePackingSlipPdf(packingData)

3. RETURN same response format as before

IMPORTANT:
- DO NOT change how orders are fetched
- DO NOT change grouping logic
- DO NOT change response structure

--------------------------------------------------
STEP 4 — MAP P2 DATA → SHARED CONTRACT
--------------------------------------------------

Modify:

server/src/routes/p2Shipping.ts

FIND:

GET /packing-slips/:id/pdf

REPLACE ONLY PDF CREATION SECTION WITH:

const packingData: PackingSlipData = {
  packingSlipNumber: slip.packingSlipNumber,
  date: new Date(slip.createdAt).toLocaleDateString(),

  customerName: slip.customerName,
  customerAddress: slip.customerAddress,

  poNumber: slip.poNumber,
  lotNumber: slip.lotNumber,

  items: (slip.lineItems || []).map(item => ({
    partNumber: item.partNumber,
    description: item.partName,
    quantity: item.quantity,
    serialNumbers: item.serialNumbers
  })),

  totalQuantity: slip.totalQuantity,

  trackingNumber: slip.trackingNumber
}

const pdfBuffer = await generatePackingSlipPdf(packingData)

RETURN buffer as before

IMPORTANT:
- DO NOT change DB queries
- DO NOT change route behavior

--------------------------------------------------
STEP 5 — DO NOT DELETE OLD CODE YET
--------------------------------------------------

COMMENT OUT old inline PDF logic:

// LEGACY PACKING SLIP RENDERER — REPLACED BY generatePackingSlipPdf

--------------------------------------------------
STEP 6 — VALIDATION CHECKLIST
--------------------------------------------------

After implementation, verify:

P1:
- Multiple orders per PO render correctly
- Unit numbers show
- Specs show
- Address prints correctly

P2:
- Serial numbers show
- Lot number shows
- Tracking number shows

GLOBAL:
- Header is identical across P1 and P2
- Company name is consistent
- Layout is identical

--------------------------------------------------
STEP 7 — DO NOT TOUCH (YET)
--------------------------------------------------

Do NOT modify:
- pdf_templates
- pdf_config_settings wiring (yet)
- invoice system
- commercial invoice

Those come in next phase

--------------------------------------------------
SUCCESS CRITERIA
--------------------------------------------------

- One renderer used by both P1 and P2
- No change to data pipelines
- No regression in functionality
- Identical output format across both systems