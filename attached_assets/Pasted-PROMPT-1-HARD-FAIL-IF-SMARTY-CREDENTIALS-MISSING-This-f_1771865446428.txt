PROMPT 1 — HARD FAIL IF SMARTY CREDENTIALS MISSING

This fixes the most dangerous flaw: silent validation bypass.

You are implementing a SAFE PATCH to the Address Domain Service.

DO NOT refactor architecture.
DO NOT change database schema.
DO NOT change route structure.

OBJECTIVE:
If Smarty credentials are missing, validation must FAIL HARD instead of silently returning 'raw'.

================================================
STEP 1 — Modify validateWithSmarty
================================================

Open:
server/src/domain/address/addressService.ts

Locate validateWithSmarty().

Find this pattern:

if (!process.env.SMARTYSTREETS_AUTH_ID || !process.env.SMARTYSTREETS_AUTH_TOKEN) {
   return { status: 'raw' }
}

REPLACE IT WITH:

if (!process.env.SMARTYSTREETS_AUTH_ID || !process.env.SMARTYSTREETS_AUTH_TOKEN) {
   console.error('[ADDRESS SERVICE] Smarty credentials not configured');
   return {
      status: 'invalid',
      error: 'Address validation service not configured (missing Smarty credentials)'
   };
}

================================================
STEP 2 — Prevent 'raw' from being treated as success
================================================

Open validateAndNormalize().

Find this logic:

if (result.status === 'raw') {
   return {
      success: true,
      status: 'standardized',
      ...
   }
}

REMOVE this behavior entirely.

Replace it with:

if (result.status === 'raw') {
   return {
      success: false,
      status: 'invalid',
      error: 'Address validation did not execute'
   };
}

================================================
STEP 3 — Add Defensive Log
================================================

Inside validateAndNormalize(), add:

console.log('[ADDRESS VALIDATION RESULT]', {
   input: address,
   result
});

================================================
DELIVERABLE
================================================

Return:
1. Updated validateWithSmarty()
2. Updated validateAndNormalize()
3. Confirmation that no other code paths convert 'raw' into success
4. Diff summary