We need a forensic audit of Replit Publish Preview destructive schema warnings.

This is NOT primarily a migration-file cleanup task.

The likely issue is that Replit Publish Preview is generating a schema diff against production and surfacing destructive changes that are unrelated to the P1 OEM invoice_number work.

We must determine whether the dangerous changes come from:

A) actual SQL migration files

or

B) Replit publish preview schema diff tooling

Do not assume migrations are the problem.

==================================================
CURRENT ISSUE
==================================================

Republish shows dangerous warnings including:

- deleting barcode_printed from p2_serialized_items with live data
- dropping unrelated tables
- dropping indexes
- altering unrelated ID types
- disabling row-level security

These are NOT part of the P1 invoice_number task.

Current handwritten SQL migrations appear clean.

Migration safety tests are passing.

This strongly suggests the destructive diff may be coming from publish preview schema comparison, not migration files.

==================================================
TASK
==================================================

STEP 1 — Identify the Source of the Destructive Diff

Determine:

Are these destructive changes coming from:

1. actual checked-in SQL migration files

or

2. Replit publish preview schema diff engine

or

3. both

Be precise.

Show exact evidence.

==================================================
STEP 2 — Production Truth (Use Production DB Only)

Query the PRODUCTION database schema using deployment database tooling.

Do NOT use development DB.

Do NOT infer from local schema.

Inspect:

- shipment_records
- shipment_items
- p2_serialized_items
- kiosk_pin_rate_limits
- persistent_discounts
- order_filter_presets
- short_term_sales
- linked_order_groups
- linked_orders
- and every table listed in the publish warning

Determine:

- what actually exists
- what columns actually exist
- what indexes actually exist
- whether barcode_printed exists
- whether barcode_printed_at exists
- whether production matches schema.ts

Production DB is source of truth.

==================================================
STEP 3 — Compare Against schema.ts + Publish Preview

Determine:

1. legitimate pending work

2. schema drift

3. false-positive destructive diffs

4. publish preview auto-diff behavior

Specifically answer:

Is publish preview attempting to auto-reconcile schema drift even when SQL migrations are safe?

==================================================
STEP 4 — Safe Reconciliation Plan

Recommend:

- what should be changed in schema.ts
- what should be left alone
- what should never be auto-published
- whether publish preview warnings can be safely ignored
- whether a specific schema reconciliation commit is needed first

==================================================
STEP 5 — Safe Path for invoice_number Publish

After drift diagnosis:

Confirm the safest production path for:

ALTER TABLE shipment_records
ADD COLUMN invoice_number text;

and preserving:

- invoice persistence
- invoice reuse on reprint
- OEM shipment search by invoice #
- UI invoice display

without publishing unrelated destructive changes

==================================================
IMPORTANT
==================================================

Do NOT publish yet.

Do NOT delete migrations blindly.

Do NOT trust publish preview warnings without proving their source.

Protect production first.

Return:

1. Source of destructive diff
2. Production truth
3. Drift diagnosis
4. Whether warnings are real or false-positive
5. Safe reconciliation plan
6. Safe publish path for invoice_number
7. Whether republish can proceed safely