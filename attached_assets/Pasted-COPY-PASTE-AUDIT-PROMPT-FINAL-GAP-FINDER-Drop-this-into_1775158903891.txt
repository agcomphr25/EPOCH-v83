COPY-PASTE AUDIT PROMPT (FINAL GAP FINDER)

Drop this into your agent exactly as-is:

You are performing a READ-ONLY FORENSIC AUDIT of the UPDATED EPOCH ORDER EVENT SYSTEM.

DO NOT MODIFY ANY CODE.
DO NOT IMPLEMENT ANY FIXES.
ONLY ANALYZE AND REPORT.

==================================================
OBJECTIVE
==================================================

Determine why certain ORDER STATE TRANSITIONS (especially reverse transitions like FINALIZED → FULFILLED) are NOT appearing in the Order Story timeline.

We specifically need to understand:

1. How a status change can occur WITHOUT a corresponding visible event
2. Whether events are missing, misclassified, filtered, or failing to write
3. Whether any write paths still bypass the canonical event system

==================================================
KNOWN SYMPTOM
==================================================

In the UI:

- Multiple events show: FULFILLED → FINALIZED
- But NO event shows: FINALIZED → FULFILLED
- The system state DID change (confirmed manually)
- Therefore:
  → Either event was not written
  → Or event was written but not shown

==================================================
STEP 1 — TRACE ALL STATUS WRITE PATHS (POST-REFACTOR)
==================================================

Identify ALL remaining ways order.status or currentDepartment can change.

Search for:

- db.update(allOrders)
- updateOrder*
- transitionOrder*
- apply*Patch*
- admin override functions
- storage layer helpers
- badge scan handlers
- NCR / nonconformance
- shipping routes
- batch update routes
- offline replay

For each path:

OUTPUT:
- file
- function
- how status is changed
- whether canonical service is used
- whether event is guaranteed

Highlight:

🚨 ANY path that still directly mutates status or department

==================================================
STEP 2 — VERIFY CANONICAL SERVICE ENFORCEMENT
==================================================

Locate the canonical order change service (e.g., transitionOrderState, applyOrderPatch, etc.)

Verify:

- Does EVERY status change go through this service?
- Is it technically possible to bypass it?
- Are there any exported helpers that allow direct mutation?

Check:

- Are there guards preventing direct DB writes?
- Is the service used in ALL routes identified in Step 1?

==================================================
STEP 3 — CHECK TRANSACTIONAL INTEGRITY
==================================================

For the canonical service:

Verify:

- Is event creation and DB update in a SINGLE transaction?
- What happens if event write fails?

Answer:

- Can state update succeed while event write fails?
- Are errors swallowed anywhere?

Search for:

- try/catch blocks around event logging
- console.error without throw
- optional logging patterns

Highlight:

🚨 ANY scenario where state change can succeed without event

==================================================
STEP 4 — EVENT CLASSIFICATION + VISIBILITY
==================================================

Trace how events are classified:

- STATUS_CHANGED
- FIELD_PATCHED
- ADMIN_OVERRIDE
- SYSTEM_AUTOMATION
- BADGE_SCAN
- etc.

Then trace:

- Order Story API
- UI filters
- category filters
- tab filtering logic

Answer:

- Are some event types excluded from the Story view?
- Could reverse transitions be logged under a different event type?
- Are admin/system events hidden?

==================================================
STEP 5 — TRACE ADMIN + OVERRIDE PATHS
==================================================

Specifically inspect:

- admin override UI
- order data override tools
- bulk update routes

Determine:

- Do these use canonical service?
- Do they write events?
- What event type do they use?
- Are they visible in the timeline?

==================================================
STEP 6 — TRACE A REAL FAILURE PATH
==================================================

Reconstruct EXACTLY how this could happen:

FINALIZED → FULFILLED (no event)
FULFILLED → FINALIZED (event appears)

You must:

- identify the code path that could produce this
- explain step-by-step execution
- explain why event was not written OR not shown

==================================================
STEP 7 — EVENT STORAGE VS UI GAP
==================================================

Compare:

- raw event table contents
- API response
- UI rendering

Answer:

- Does the missing event exist in DB but not in UI?
- Or does it never get written?

==================================================
OUTPUT FORMAT
==================================================

Provide:

1. REMAINING BYPASS PATHS
2. CANONICAL SERVICE GAPS
3. TRANSACTIONAL FAILURES (CRITICAL)
4. EVENT CLASSIFICATION / FILTER ISSUES
5. ADMIN / OVERRIDE GAPS
6. FAILURE SCENARIO RECONSTRUCTION
7. ROOT CAUSE (why FINALIZED → FULFILLED is missing)

DO NOT FIX.
DO NOT WRITE CODE.

==================================================
SUCCESS CRITERIA
==================================================

We should clearly know:

- whether events are missing vs hidden
- exactly which code path caused the invisible transition
- whether hard enforcement is truly complete or still partially bypassed