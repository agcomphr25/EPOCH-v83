READ-ONLY AUDIT ONLY.
DO NOT MODIFY ANY FILES.
DO NOT WRITE CODE.
DO NOT APPLY PATCHES.
DO NOT CREATE COMMITS.
DO NOT REFACTOR.
ONLY inspect the current EPOCH codebase and produce a forensic audit report.

I need you to investigate how P1 PO packing slips and invoice PDF configurations currently work in EPOCH.

## BUSINESS CONTEXT

We have struggled to get PO orders to behave consistently, but we are much closer now.
At the moment, I am noticing that different packing slips are being created for different customers.
I need to understand the current codebase and determine how to:

1. make packing slips more consistent across customers,
2. allow admin/user-driven styling customization for the packing slip,
3. allow me to dictate which invoice configuration should be used on invoices,
4. understand what “PDF centralization” / “PDF customization” infrastructure already exists and whether it can be reused here.

I am currently lost on what we already have versus what is actually wired.

## PRIMARY AUDIT GOALS

Please perform a complete READ-ONLY audit of:

- P1 PO packing slip generation
- invoice PDF generation/configuration
- customer-specific template branching
- any centralized PDF/template system
- any admin-configurable styling or document-layout system
- any config resolution logic that decides which template/config gets used

## WHAT I NEED ANSWERED

### A. PACKING SLIP CURRENT STATE
Find exactly:

1. Where packing slips are generated
2. Which routes/endpoints/components trigger packing slip generation
3. Which server-side PDF/template files are used
4. What data payload is passed into the packing slip
5. Whether there are different code paths for different customers
6. Whether the differences are driven by:
   - customer name checks
   - customer IDs
   - customer settings
   - order type
   - P1 vs P2 logic
   - document config tables
   - hardcoded conditionals
   - feature flags
   - legacy fallback code
7. Whether multiple packing slip template systems exist in parallel

### B. INVOICE CURRENT STATE
Find exactly:

1. Where invoice PDFs are generated
2. How the system decides which invoice configuration/template is used
3. Whether invoice configuration is:
   - global
   - customer-specific
   - order-type-specific
   - tenant-specific
   - hardcoded
4. Whether there is already a notion of “invoice config”, “document config”, “branding config”, or similar
5. Whether invoices and packing slips share any common PDF infrastructure or if they are duplicated

### C. PDF CENTRALIZATION / CUSTOMIZATION
Investigate whether we already have any partially built or unused infrastructure for:

- centralized PDF rendering
- centralized document templates
- shared PDF utilities
- style tokens / brand tokens for PDFs
- admin document settings
- per-customer document configuration
- document layout definitions
- document theming
- document overrides
- template registries
- document type mapping
- print / export services

I specifically want to know:
- what exists,
- what is actually used,
- what appears partially implemented but not wired,
- what is dead code / legacy / duplicate.

### D. CUSTOMIZATION FEASIBILITY
Assess how hard it would be to support:

1. a consistent baseline packing slip across all customers,
2. customer-specific optional overrides,
3. admin-controlled packing slip styling,
4. admin-controlled invoice configuration selection,
5. a unified “document config” model for both invoices and packing slips.

## SEARCH PLAN

Search broadly and thoroughly through:

- client/
- server/
- shared/
- db/schema files
- drizzle schema files
- routes
- services
- utils
- print/pdf/export modules
- admin settings UI
- customer settings UI
- document settings UI
- packing slip components
- invoice components
- purchase order / PO / P1 / P2 areas
- any template folders
- any PDF helper folders
- any branding/theme/config folders

## KEY SEARCH TERMS

Search for all relevant occurrences of terms like:

packing slip
packingslip
packing_slip
invoice
pdf
print
document
template
branding
theme
style
customer config
document config
invoice config
pdf config
renderPdf
generatePdf
pdfkit
puppeteer
jspdf
react-pdf
html-to-pdf
packing slip template
invoice template
purchase order invoice
PO invoice
P1
P2
shipment document
fulfillment document
export document
document settings
print settings
customer settings
branding settings

Also search for:
- customer-specific `if` statements
- switch/case logic by customer
- config lookup helpers
- any “centralized pdf” services
- any legacy/duplicate print functions

## REQUIRED OUTPUT FORMAT

Produce the report in the exact structure below.

# 1. EXECUTIVE SUMMARY
Give a concise summary of:
- how packing slips currently work,
- why different customers are getting different outputs,
- whether there is already centralized infrastructure,
- the best likely path to make this consistent/customizable.

# 2. PACKING SLIP FLOW MAP
Show the end-to-end flow:
UI trigger / route / API / service / template / renderer / output

For every step, include:
- file path
- function/component name
- what it does

# 3. INVOICE FLOW MAP
Show the end-to-end flow:
UI trigger / route / API / service / template / renderer / config resolution / output

For every step, include:
- file path
- function/component name
- what it does

# 4. CUSTOMER-SPECIFIC BRANCHING
List every place where document behavior changes by customer.
For each one include:
- file path
- function name
- exact customer discriminator used
- whether it affects packing slips, invoices, or both
- whether it looks intentional, legacy, brittle, or duplicate

# 5. EXISTING PDF CENTRALIZATION INFRASTRUCTURE
Identify all shared PDF/document systems.
For each one include:
- file path
- purpose
- whether actively used
- what document types use it
- whether it looks reusable for packing slips/invoices

# 6. CONFIGURATION SOURCES
Identify every place document configuration can come from:
- database tables
- constants
- environment variables
- settings screens
- customer records
- tenant/org settings
- hardcoded defaults

Explain precedence if determinable:
which source wins when multiple configs exist?

# 7. STYLING / BRANDING CUSTOMIZATION
Identify all current styling/theming hooks that could influence PDFs.
For each one include:
- file path
- what styling options exist
- whether admin-editable
- whether customer-specific
- whether actually wired to packing slips/invoices

# 8. DUPLICATION / FRAGMENTATION FINDINGS
Call out all duplicate template systems, duplicate renderers, or shadow/legacy flows.
I want a blunt assessment of where the document architecture is fragmented.

# 9. GAP ANALYSIS AGAINST MY GOALS
Evaluate the current codebase against these goals:

Goal A: Consistent packing slips across customers
Goal B: Packing slip styling customization
Goal C: Dictate invoice configuration selection
Goal D: Reuse centralized PDF customization architecture

For each goal, say:
- Fully supported
- Partially supported
- Not supported
- Unclear / conflicting

Then explain why.

# 10. RECOMMENDED TARGET ARCHITECTURE
Without writing code, recommend the cleanest architecture for:
- one shared document rendering system
- per-document-type configs
- optional per-customer overrides
- style tokens / branding layer
- deterministic config resolution for invoices and packing slips

This should be conceptual only, not implementation.

# 11. LOW-RISK IMPLEMENTATION PLAN
Provide a phased plan:
Phase 1 = forensic cleanup / truth declaration
Phase 2 = centralize config resolution
Phase 3 = unify packing slip templates
Phase 4 = styling customization
Phase 5 = invoice config selection/admin control

For each phase include:
- what would be changed
- dependencies
- likely risks
- likely regressions to test

# 12. FILES TO REVIEW FIRST
Give me the top 15-25 most important files, ordered by relevance.

For each file include:
- exact path
- why it matters
- what question it helps answer

## IMPORTANT RULES

- Read the whole relevant flow, not just keyword hits
- Do not assume a file is unused without tracing references
- Distinguish clearly between:
  - actively wired production flow
  - partially implemented but unused flow
  - dead/legacy code
- If there are multiple competing systems, explain which one appears to be the real source of truth
- Be explicit about uncertainty
- Quote relevant code snippets only when necessary
- Prefer precise file/function references over broad summaries

Again:
READ-ONLY ONLY.
NO CODE CHANGES.
NO PATCHES.
NO COMMITS.